// calculate the area of the triangle

const base = 4;
const height = 5;
const area = base*height /2;
console.log(area);
console.log(" this is area of triangle " + area + " and its " + base + "and its" + height);
console.log(`the area of triangle is ${area}`);